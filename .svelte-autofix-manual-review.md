# Svelte Autofixer — Manual Review

These 14 files were flagged for manual review during the autofixer batch run. The autofixer suggests refactoring `$effect` patterns to `$derived`, but in these cases that change would break behavior.

## Files and Reasons

| File                                                            | Reason                                                           |
| --------------------------------------------------------------- | ---------------------------------------------------------------- |
| `src/routes/+layout.svelte`                                     | `$effect` hydrates theme/transition prefs and header height sync |
| `src/lib/components/admin/photos/AdminPhotoCard.svelte`         | Syncs server form values into local form state                   |
| `src/lib/components/admin/SveditEditor.svelte`                  | `$effect` syncs `loadError`/`value` for editor                   |
| `src/lib/components/gallery/GalleryScene.svelte`                | Route/preloader sync in `$effect`                                |
| `src/lib/components/HomeSlideshow.svelte`                       | Slideshow slots, timers, transitions in `$effect`                |
| `src/lib/components/navigation/MobileDropdownMenu.svelte`       | DOM side effects (body overflow, inert, focus)                   |
| `src/lib/components/admin/ThumbnailCropEditor.svelte`           | Syncs initial crop into draggable crop state                     |
| `src/routes/admin/categories/+page.svelte`                      | Form value sync for create form                                  |
| `src/routes/admin/galleries/+page.svelte`                       | Form value sync for create form                                  |
| `src/routes/admin/pages/+page.svelte`                           | Form value sync for create form                                  |
| `src/routes/admin/pages/edit/[identifier]/+page.svelte`         | Form value sync for edit form                                    |
| `src/routes/admin/[gallerySlug]/details/+page.svelte`           | `visibilityStatus` sync from form                                |
| `src/routes/admin/homepage/+page.svelte`                        | Slides selection sync for drag/drop editor                       |
| `src/lib/components/admin/photos/AdminSinglePhotoEditor.svelte` | Taxonomy/categories sync into local state                        |

## Risk

Applying the autofixer’s suggestions here would be unsafe because:

- **Form fields** rely on writable state for `bind:value`; `$derived` values are read-only.
- **Drag-and-drop and crop editors** need mutable state for user interactions.
- **Theme/transition hydration** must run once on the client; it cannot be expressed as a pure derivation.

Only refactor these after a careful review to ensure behavior is preserved.
